#!/bin/bash
    # Скрипт на проверку файлов <3
echo "Какой файл ищем?: $1"

if [ -f "$1" ]; then 
    echo "Файл $1 найден! По пути: $(realpath "$1")"
else  
    echo "Файл не найден.."
    # echo "Файлн не найден.. Ща создам))  $(touch "$1")"
fi

